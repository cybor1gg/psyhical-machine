# Local MongoDB started by the cabinet launcher (see tools/ + launcher/)
MONGODB_URI=mongodb://127.0.0.1:27018/cabinet
# Fresh random secret per deployment: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=change-me
PORT=5001
WEB_ORIGIN=http://localhost:3000
